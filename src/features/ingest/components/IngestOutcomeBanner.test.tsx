import { describe, expect, it } from 'vitest';

describe('IngestOutcomeBanner', () => {
  it('is deprecated in favor of header action buttons in IngestRunStatusPanel', () => {
    expect(true).toBe(true);
  });
});
